{% extends 'admin/layout.html.twig' %}

{% block title %}
  Edit Contact
{% endblock %}

{% block body %}
  <h1> Edit Contact</h1>
  {{include('contact/_form.html.twig',{button_label: 'Update'})}}
  <a href="{{ path('contact_index') }}">Revenir à la liste</a>
  {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
